

### 🧱 What is HTML?

**HTML** stands for **HyperText Markup Language**.
It’s not a programming language — it’s a **markup language** that describes the **structure and meaning of content** on a web page.

Think of it as the blueprint of a building:

* HTML says *what’s there* — paragraphs, images, buttons, links, forms, etc.
* CSS says *how it looks*.
* JavaScript says *what it does*.

When a browser loads a page, it reads your HTML file from top to bottom and creates a **DOM (Document Object Model)** — a tree of elements the browser uses to display and interact with your page.

---

### ⚙️ How HTML Works

1. You write an HTML file (usually called `index.html`).
2. The browser parses it line by line.
3. It builds the **DOM Tree** — a live representation of the page structure.
4. CSS and JavaScript later interact with this DOM to style or manipulate content.

So every visible element on a webpage — from text to video — starts as HTML.

---

### 🧾 Latest Version

The latest major version is **HTML5**, standardized by the **WHATWG** (Web Hypertext Application Technology Working Group).

HTML5 introduced:

* Semantic elements like `<header>`, `<footer>`, `<nav>`, `<article>`, `<section>`
* Multimedia support (`<video>`, `<audio>`)
* Form enhancements (`<input type="email">`, `<datalist>`)
* `<canvas>` for graphics
* API support (Geolocation, Web Storage, Drag-and-drop)

HTML5 isn't static — it's a **living standard**, continuously updated.

---

### ✍️ How to Write HTML

HTML files are plain text files that end with `.html`.
You can write them using any text editor — even Notepad.

Here’s a minimal example:

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My First Webpage</title>
  </head>
  <body>
    <h1>Hello, world!</h1>
    <p>Welcome to my very first webpage.</p>
  </body>
</html>
```

---

### 🏷️ Tags and Elements

**Tags** tell the browser what kind of content something is.
They usually come in **pairs**:

```html
<p>This is a paragraph.</p>
```

* `<p>` → opening tag
* `</p>` → closing tag
* `This is a paragraph.` → content
  Together, that’s an **element**.

Some tags don’t have closing pairs — they’re **self-closing**:

```html
<img src="image.jpg" alt="A photo of the moon" />
<br />
<hr />
```

---

### 🧩 Common & Basic Tags

| Purpose         | Tag                         | Description                            |
| --------------- | --------------------------- | -------------------------------------- |
| Document type   | `<!DOCTYPE html>`           | Declares HTML5 document                |
| Root element    | `<html>`                    | Wraps entire HTML document             |
| Metadata        | `<head>`                    | Contains title, links, scripts         |
| Title           | `<title>`                   | Sets page title (shown in browser tab) |
| Visible content | `<body>`                    | Holds all the page content             |
| Headings        | `<h1>`–`<h6>`               | Defines headings from large to small   |
| Paragraph       | `<p>`                       | Text block                             |
| Links           | `<a href="...">`            | Creates a hyperlink                    |
| Image           | `<img src="..." alt="...">` | Displays an image                      |
| Lists           | `<ul>`, `<ol>`, `<li>`      | Unordered/ordered lists                |
| Division        | `<div>`                     | Generic container                      |
| Span            | `<span>`                    | Inline container                       |
| Line break      | `<br>`                      | Breaks line                            |
| Emphasis        | `<strong>`, `<em>`          | Bold / italic emphasis                 |
| Input           | `<input type="text">`       | User input fields                      |
| Button          | `<button>`                  | Clickable button                       |

---

### 💡 Tips for Writing Clean HTML

* Always start with `<!DOCTYPE html>` (tells browser to use HTML5 mode).
* Use proper **indentation** — makes structure readable.
* Close all tags properly (even optional ones like `<li>`).
* Use **semantic HTML** — prefer `<header>` over `<div class="header">`.
* Add `alt` text for images (for accessibility and SEO).
* Use lowercase for tag and attribute names.
* Don’t stuff everything in `<div>` — divs are for grouping when no semantic tag fits.
* Validate your HTML using [validator.w3.org](https://validator.w3.org/).

---

HTML is deceptively simple — it’s just text wrapped in tags — but it’s the foundation of everything on the web.
Without it, the browser has no idea what to display.

---

