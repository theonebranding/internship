
# 🧠 **DAY 3 — PART 3.2 — OBJECTS (DEEP DIVE)**

---

# 🎯 **3.2.1 — What Is an Object? (Simple Explanation)**

An **object** stores information in **key–value pairs** (like a dictionary).

Real life examples:

* A student profile
* A product card
* A user account
* A bank customer record
* A book information page

Example:

```js
let student = {
  name: "Rahil",
  age: 21,
  isPass: true
};
```

Keys → name, age, isPass
Values → "Rahil", 21, true

---

# 🎟️ **3.2.2 — Why Do We Need Objects?**

Variables and arrays suck at representing **structured data**.

For example, storing a student's details:

❌ BAD (using variables):

```js
let name = "Rahil";
let age = 21;
let marks = 90;
```

❌ BAD (using array):

```js
let student = ["Rahil", 21, 90];
```

Hard to remember what index 0, 1, 2 means.

✔ BEST (using object):

```js
let student = {
  name: "Rahil",
  age: 21,
  marks: 90
};
```

Clear, readable, scalable.

---

# 🛠️ **3.2.3 — Creating Objects (4 Ways)**

### ✔ Method 1 — Object Literal (Most used)

```js
let user = {
  name: "Ayan",
  age: 20
};
```

### ✔ Method 2 — new Object()

```js
let user = new Object();
user.name = "Ayan";
user.age = 20;
```

### ✔ Method 3 — Using a function (factory)

```js
function createUser(name, age) {
  return { name, age };
}

let u1 = createUser("Rahil", 21);
```

### ✔ Method 4 — Using a constructor function

(more advanced, used before classes)

```js
function User(name, age) {
  this.name = name;
  this.age = age;
}

let u1 = new User("Ayan", 20);
```

---

# 🔍 **3.2.4 — Accessing Object Values**

### ✔ Dot Notation (most common)

```js
console.log(user.name);
```

### ✔ Bracket Notation (for dynamic keys)

```js
console.log(user["age"]);
```

Useful when:

```js
let key = "name";
console.log(user[key]);
```

---

# ✏️ **3.2.5 — Add, Update, Delete Properties**

### ✔ Add new key

```js
user.city = "Mumbai";
```

### ✔ Update existing key

```js
user.age = 25;
```

### ✔ Delete key

```js
delete user.age;
```

---

# 🧰 **3.2.6 — Nested Objects**

Objects can contain:

* Arrays
* Functions
* Other objects

Example:

```js
let profile = {
  username: "rahil123",
  age: 21,
  social: {
    instagram: "@rahil",
    twitter: "@rahil_dev"
  }
};

console.log(profile.social.instagram);
```

---

# 🧱 **3.2.7 — Objects Inside Arrays (Real World)**

Almost every API response is like this:

```js
let products = [
  { id: 1, name: "Laptop", price: 70000 },
  { id: 2, name: "Phone", price: 20000 },
  { id: 3, name: "Camera", price: 35000 }
];

console.log(products[1].name);  // Phone
```

---

# 🔁 **3.2.8 — Looping Through Objects**

### ✔ for…in (best for objects)

```js
for (let key in user) {
  console.log(key, user[key]);
}
```

### ✔ Object.keys()

```js
console.log(Object.keys(user));
```

### ✔ Object.values()

```js
console.log(Object.values(user));
```

### ✔ Object.entries()

```js
console.log(Object.entries(user));
```

---

# 🟦 **3.2.9 — Check if Key Exists**

### Using `in`

```js
console.log("age" in user);
```

### Using undefined check

```js
if (user.city !== undefined) console.log("City exists");
```

---

# 🟪 **3.2.10 — Objects Are Stored by Reference**

This is IMPORTANT.

```js
let a = { value: 10 };
let b = a;

b.value = 20;

console.log(a.value); // 20
```

Because **a and b point to the SAME object**.

---

# 🚀 **3.2.11 — Spread Operator with Objects (Useful!)**

Used to **copy** objects.

```js
let user = { name: "Rahil", age: 21 };
let copyUser = { ...user };

copyUser.age = 30;

console.log(user.age);  // 21
console.log(copyUser.age); // 30
```

---

# 🧩 **3.2.12 — Useful Real-Life Object Examples**

---

### 🛒 **E-Commerce Product**

```js
let product = {
  name: "iPhone",
  price: 80000,
  stock: true,
  discount: 10,
  finalPrice: function() {
    return this.price - (this.price * this.discount / 100);
  }
};
```

---

### 🎟 **User Profile**

```js
let user = {
  username: "rahil_dev",
  followers: 120,
  verified: true
};
```

---

### 🎬 **Movie Object**

```js
let movie = {
  title: "Interstellar",
  rating: 8.6,
  actors: ["Matthew", "Anne", "Casey"]
};
```

---

### 🧾 **Invoice Object**

```js
let invoice = {
  id: 101,
  items: [
    { name: "Shoes", price: 2000 },
    { name: "Cap", price: 500 }
  ]
};
```

---

# 🎉 **3.2 Complete — Objects Mastered!**

You now understand:

✔ What objects are
✔ Keys and values
✔ Creating/editing/deleting
✔ Nested objects
✔ Objects in arrays
✔ Looping through objects
✔ Reference behavior
✔ Spread operator
✔ Real-world structures

This is 80% of real-life frontend & backend coding.

---


Just say **“3.3 now”**.
