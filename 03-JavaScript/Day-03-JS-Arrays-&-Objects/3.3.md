# 🧠 **DAY 3 — PART 3.3**

# **Arrays + Objects Together (Real-World Data Handling)**

This is where every real application lives.

Whenever you work with:

* APIs
* Databases
* Shopping carts
* Notifications
* Product lists
* Users
* Employees
* Orders

…you always get an **Array of Objects** or **Objects containing Arrays**.

This section will teach interns how to handle real data.

Let’s deep dive.

---

# 🎯 **3.3.1 — What Does "Array + Object Together" Mean?**

It means:

### ✔ Objects inside an array

Example:
A list of products
A list of students
A list of posts

### ✔ Arrays inside an object

Example:
A user with list of hobbies
A product with multiple images
A course with multiple chapters

### ✔ Nested combinations

Example:
An order that contains:

* user object
* cart array
* each cart item = object

This builds real websites.

---

# 🟦 **3.3.2 — Array of Objects (MOST IMPORTANT)**

This is how APIs return data.

Example: List of products:

```js
let products = [
  { id: 1, name: "Laptop", price: 50000 },
  { id: 2, name: "Mobile", price: 20000 },
  { id: 3, name: "Camera", price: 35000 }
];
```

Access second product:

```js
console.log(products[1].name); // Mobile
```

---

# 🟩 **3.3.3 — Looping Through Array of Objects**

Using `for…of`:

```js
for (let p of products) {
  console.log(p.name, "→ ₹" + p.price);
}
```

Using classic loop:

```js
for (let i = 0; i < products.length; i++) {
  console.log(products[i].price);
}
```

Real-life use: Display product cards on a website.

---

# 🟧 **3.3.4 — Searching in Array of Objects**

Example: Find product with price > 30000

```js
for (let item of products) {
  if (item.price > 30000) {
    console.log(item.name);
  }
}
```

Example: Find Laptop

```js
for (let p of products) {
  if (p.name === "Laptop") {
    console.log("Found:", p);
  }
}
```

---

# 🟥 **3.3.5 — Adding a New Object to Array**

Add a new product:

```js
products.push({ id: 4, name: "Tablet", price: 15000 });
```

---

# 🟪 **3.3.6 — Removing an Object (Using splice)**

```js
products.splice(1, 1); // removes "mobile"
```

---

# 🟫 **3.3.7 — Updating an Object Inside Array**

```js
products[0].price = 45000;
```

Real-life: Admin dashboard changing product price.

---

# 🌱 **3.3.8 — Arrays Inside Objects**

Example: User profile with hobbies:

```js
let user = {
  name: "Rahil",
  age: 21,
  hobbies: ["Cricket", "Gaming", "Coding"]
};
```

Access hobby:

```js
console.log(user.hobbies[1]); // Gaming
```

Loop through hobbies:

```js
for (let h of user.hobbies) {
  console.log(h);
}
```

---

# 🧱 **3.3.9 — Nested Objects + Arrays (Real-World Example)**

Let’s simulate Amazon Cart:

```js
let order = {
  orderId: 101,
  customer: {
    name: "Rahil",
    address: "Mumbai"
  },
  items: [
    { name: "Shoes", price: 2000, qty: 2 },
    { name: "Watch", price: 1500, qty: 1 }
  ]
};
```

Accessing data:

## Customer name

```js
console.log(order.customer.name);
```

## Price of first item

```js
console.log(order.items[0].price);
```

## Loop through items

```js
for (let item of order.items) {
  console.log(item.name, item.qty, item.price);
}
```

---

# 🧮 **3.3.10 — Real-Life: Calculate Order Total**

```js
let total = 0;

for (let item of order.items) {
  total += item.qty * item.price;
}

console.log("Order Total:", total);
```

This exact code appears in:

* E-commerce websites
* Billing apps
* POS systems

---

# 🎬 **3.3.11 — Movies Example (Array of Objects)**

```js
let movies = [
  { title: "Inception", rating: 8.8, year: 2010 },
  { title: "Interstellar", rating: 8.6, year: 2014 },
  { title: "Dunkirk", rating: 7.9, year: 2017 }
];

for (let movie of movies) {
  console.log(movie.title + " (" + movie.year + ")");
}
```

---

# 🪪 **3.3.12 — Students Example (Most Useful for Interns)**

```js
let students = [
  { name: "Ayan", marks: [85, 90, 92] },
  { name: "Sana", marks: [78, 81, 80] }
];

console.log(students[0].marks[1]); // 90
```

—

# 🛠️ **3.3.13 — Real-Life: Notifications List**

```js
let notifications = [
  { message: "New like", time: "2 min ago" },
  { message: "New follower", time: "5 min ago" }
];

for (let n of notifications) {
  console.log(n.message);
}
```

---

# 🔥 **3.3.14 — Summary of All Patterns**

### ✔ Array of objects → `[{}, {}, {}]`

### ✔ Object containing arrays → `{arr: []}`

### ✔ Nested objects → `{obj: {}}`

### ✔ Nested arrays → `[[], []]`

### ✔ Complex real apps → mix of all above

Once you understand these patterns,
you can easily work with **APIs**, **React**, **Node.js**, and **databases**.

