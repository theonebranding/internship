# Write a code to do following tasks

# ✅ **TASK 1 — Check if Number is Positive, Negative, or Zero (Easy)**

### **Description**

Write a function that checks whether a number is:

* `"positive"`
* `"negative"`
* `"zero"`

### **Hint**

Use simple if–else conditions.

### ❌ **Do NOT use:**

* Math.sign()

### ✔ **Test Cases**

| Input | Output       |
| ----- | ------------ |
| `5`   | `"positive"` |
| `0`   | `"zero"`     |
| `-3`  | `"negative"` |

---

# ✅ **TASK 2 — Convert Minutes to Seconds (Easy)**

### **Description**

Write a function that converts minutes into seconds.

### **Hint**

Multiply minutes × 60.

### ❌ **Do NOT use:**

* Floating conversions
* Libraries

### ✔ **Test Cases**

| Input | Output |
| ----- | ------ |
| `5`   | `300`  |
| `1`   | `60`   |
| `0`   | `0`    |

---

# ✅ **TASK 3 — Return First and Last Element (Easy)**

### **Description**

Write a function that returns an object:

```js
{ first: ..., last: ... }
```

from an array.

### **Hint**

Use `arr[0]` and `arr[arr.length - 1]`.

### ❌ **Do NOT use:**

* Array destructuring
* .shift() / .pop()

### ✔ **Test Cases**

| Input          | Output                              |
| -------------- | ----------------------------------- |
| `[10, 20, 30]` | `{first:10, last:30}`               |
| `[1]`          | `{first:1, last:1}`                 |
| `[]`           | `{first:undefined, last:undefined}` |

---

# ✅ **TASK 4 — Count Characters in a String (Easy)**

### **Description**

Count the number of characters in a string (including spaces).

### **Hint**

Use `.length`.

### ❌ **Do NOT use:**

* Regex
* Looping through characters

### ✔ **Test Cases**

| Input        | Output |
| ------------ | ------ |
| `"hello"`    | `5`    |
| `"hi rahil"` | `8`    |
| `""`         | `0`    |

---

# ✅ **TASK 5 — Multiply All Items in Array (Easy)**

### **Description**

Return the product of all numbers in an array.

### **Hint**

Start with `result = 1`.

### ❌ **Do NOT use:**

* reduce()

### ✔ **Test Cases**

| Input     | Output                   |
| --------- | ------------------------ |
| `[1,2,3]` | `6`                      |
| `[5]`     | `5`                      |
| `[]`      | `1` (empty product rule) |

---

# 🟡 **TASK 6 — Find Missing Number (Medium)**

### **Description**

Given an array containing numbers 1 to N but missing one number, return the missing number.

Example: `[1,2,4,5] → 3`

### **Hint**

Use sum of series formula:
`N * (N+1) / 2`

### ❌ **Do NOT use:**

* Sorting
* Sets

### ✔ **Test Cases**

| Input       | Output             |
| ----------- | ------------------ |
| `[1,2,4,5]` | `3`                |
| `[2,3,1,5]` | `4`                |
| `[1]`       | `2` (assuming N=2) |

---

# 🟢 **TASK 7 — Recursion: Reverse a String (Medium)**

### **Description**

Write a recursive function to reverse a string.

Example:
`"rahil" → "lihar"`

### **Hint**

Return last char + recursion of remaining chars.

### ❌ **Do NOT use:**

* loops
* `.reverse()`
* array conversions

### ✔ **Test Cases**

| Input     | Output    |
| --------- | --------- |
| `"abc"`   | `"cba"`   |
| `"hello"` | `"olleh"` |
| `""`      | `""`      |

---

# 🔥 **TASK 8 — Hard: Merge & Sort Two Arrays Without Built-in Sort (No Recursion)**

### **Description**

Write a function that:

1. Takes **two arrays**
2. Merges them
3. Sorts them **manually** (ascending)
4. Returns the new sorted array

### **Hint**

Use a simple sorting algorithm like bubble sort or selection sort.

### ❌ **Do NOT use:**

* `.sort()`
* recursion
* Set
* flatten()

### ✔ **Test Cases**

| Input                | Output         |
| -------------------- | -------------- |
| `[3,1,4]` + `[2,5]`  | `[1,2,3,4,5]`  |
| `[10,7]` + `[8,6,9]` | `[6,7,8,9,10]` |
| `[]` + `[1,2]`       | `[1,2]`        |

---


