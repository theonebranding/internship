
# ✅ **25 MCQ — HTML + CSS + JS + DOM (Interview Balanced Level)**

---

### **1. Which HTML tag is used to define important text?**

A. `<strong>`
B. `<b>`
C. `<important>`
D. `<em>`



---

### **2. What does the `alt` attribute in `<img>` do?**

A. Adds border
B. Adds title
C. Provides text if image fails to load
D. Sets image size



---

### **3. Which HTML element is semantic?**

A. `<div>`
B. `<span>`
C. `<section>`
D. `<b>`



---

### **4. What does CSS stand for?**

A. Computer Style Sheets
B. Cascading Style Sheets
C. Creative Styling System
D. Color Styling Sheets



---

### **5. Which CSS property controls text size?**

A. font-weight
B. text-size
C. size
D. font-size



---

### **6. Which CSS property is used to change background color?**

A. bg-color
B. color-background
C. background-color
D. background-style



---

### **7. What does `display: inline-block;` allow?**

A. Elements ignore width/height
B. Elements take full width
C. Inline behavior with width/height allowed
D. Hidden element



---

### **8. Which CSS unit is relative to the root font-size?**

A. px
B. em
C. rem
D. %


---

### **9. What will `position: absolute;` position the element relative to?**

A. Browser window only
B. Nearest positioned ancestor
C. Top of document always
D. Its own width



---

### **10. Which property makes an element come above another?**

A. order
B. priority
C. over
D. z-index



---

### **11. What does JavaScript primarily run on?**

A. CPU directly
B. Browser JavaScript engine
C. Operating system
D. GPU



---

### **12. How many threads does JavaScript use for execution?**

A. 2
B. 4
C. Single thread
D. Multi-thread



---

### **13. What is the correct way to write a function in JS?**

A. `function: myFunc()`
B. `function myFunc()`
C. `func myFunc()`
D. `create function myFunc()`



---

### **14. What will `typeof null` return?**

A. "null"
B. "object"
C. "undefined"
D. "boolean"



---

### **15. Which method selects an element by ID in DOM?**

A. `querySelectorAll()`
B. `getElementsByClassName()`
C. `getElementById()`
D. `selectById()`



---

### **16. `innerText` returns:**

A. All HTML including tags
B. Only text visible to the user
C. Styles applied
D. Node attributes



---

### **17. What does `event.preventDefault()` do?**

A. Stops all scripts
B. Prevents browser default action
C. Reloads page
D. Stops event forever



---

### **18. What does `===` check in JS?**

A. Only value
B. Only type
C. Type + value
D. Reference only



---

### **19. What is the default display of a `<div>`?**

A. inline
B. flex
C. block
D. none



---

### **20. What is the purpose of `async` keyword?**

A. Makes code multi-threaded
B. Immediately executes function
C. Makes a function return a Promise
D. Slows down execution



---

### **21. Which runs first in JS execution?**

A. setTimeout callback
B. Promise microtask
C. DOM events
D. interval callback



---

### **22. How do you prevent layout shift while animating?**

A. Animate width
B. Animate padding
C. Animate transform
D. Animate margin


---

### **23. In CSS, `overflow: hidden;` means:**

A. Content is scrollable
B. Content is clipped (not visible)
C. Extra content becomes gray
D. Adds scrollbar



---

### **24. `document.createElement("div")` does what?**

A. Inserts div into page
B. Creates div in memory
C. Deletes node
D. Replaces node


(You must append it manually)

---

### **25. What is the correct order of the event loop?**

A. Microtasks → Call Stack → Macrotasks
B. Call Stack → Microtasks → Macrotasks
C. Macrotasks → Microtasks → Call Stack
D. Call Stack → Macrotasks → Microtasks



