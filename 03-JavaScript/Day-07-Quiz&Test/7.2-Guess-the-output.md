

# Guess The Output

# 🔥 **PROBLEM 1 — **

```js
console.log(a);
var a = 10;

console.log(b);
let b = 20;

console.log(foo());
function foo() { return 99; }
```

---

# 🔥 **PROBLEM 2 — **

```js
let x = { value: 1 };
let y = x;

x.value = 5;
y = { value: 10 };

console.log(x.value, y.value);
```

---


# 🔥 **PROBLEM 3 — **

```js
console.log(typeof 10);
console.log("10" - 2);
console.log("10" + 2 - 1);
console.log(typeof typeof 10);
```

---

# 🔥 **PROBLEM 5 — **

```js
let user = { name: "Rahil", age: 21 };

(function({ name, age }) {
  let name2 = name;
  name = "Changed";
  console.log(name, name2, user.name);
})(user);
```

---

# 🌀 **SECTION 2 — 10  Async Execution Problems**


---

# 🔥 **PROBLEM 6 — **

```js
console.log("A");

setTimeout(() => {
  console.log("B");

  async function run() {
    console.log("C");
    await Promise.resolve();
    console.log("D");
  }

  run();
}, 0);

Promise.resolve().then(() => console.log("E"));

console.log("F");
```

---

# 🔥 **PROBLEM 7 — **

```js
console.log(1);

async function x() {
  console.log(2);
  await null;
  console.log(3);
}

x();

Promise.resolve().then(() => console.log(4));

setTimeout(() => console.log(5));

console.log(6);
```

---

# 🔥 **PROBLEM 8 — **

```js
async function test() {
  console.log("X");
  await Promise.resolve();
  console.log("Y");

  await Promise.resolve(
    Promise.resolve().then(() => console.log("Z"))
  );

  console.log("W");
}

console.log("A");
test();
console.log("B");
```

---

# 🔥 **PROBLEM 9 — **

```js
console.log("1");

setTimeout(() => {
  console.log("2");

  Promise.resolve()
    .then(() => console.log("3"))
    .then(() => console.log("4"));
}, 0);

Promise.resolve().then(() => console.log("5"));

console.log("6");
```

---

# 🔥 **PROBLEM 10 —**

```js
Promise.resolve().then(() => console.log("M1"));

queueMicrotask(() => {
  console.log("M2");
  Promise.resolve().then(() => console.log("M3"));
});

console.log("S1");

Promise.resolve().then(() => console.log("M4"));

console.log("S2");
```

---

# 🔥 **PROBLEM 11 **

```js
async function work(n) {
  return n * 2;
}

console.log("Start");

[1, 2, 3].forEach(async (x) => {
  console.log("A" + x);
  console.log(await work(x));
});

console.log("End");
```

---

# 🔥 **PROBLEM 12 **

```js
console.log("Q");

setTimeout(async () => {
  console.log("W");
  await null;
  console.log("E");
}, 0);

Promise.resolve().then(() => console.log("R"));

console.log("T");
```

---

# 🔥 **PROBLEM 13 **

```js
async function one() {
  console.log("1");
  await null;
  console.log("2");
}

async function two() {
  console.log("3");
  await null;
  console.log("4");
}

console.log("0");

one();
two();

console.log("5");
```

---

# 🔥 **PROBLEM 14 **

```js
console.log("A");

new Promise((res) => {
  console.log("B");
  res();
}).then(() => console.log("C"));

async function go() {
  console.log("D");
  await null;
  console.log("E");
}

go();

console.log("F");
```

---

# 🔥 **PROBLEM 15 — **

```js
console.log("1");

setTimeout(() => console.log("2"));

Promise.resolve().then(() => {
  console.log("3");
  setTimeout(() => console.log("4"));
});

Promise.resolve().then(() => console.log("5"));

console.log("6");
```

---


# 🔥 **PROBLEM 16 — r**

```js
function counter() {
  let n = 0;
  return function count(x) {
    if (x === 0) return n;
    n += x;
    return count(x - 1);
  };
}

const c = counter();

console.log(c(3));  
console.log(c(2));  
```

---

# 🔥 **PROBLEM 17 — **

```js
function a() {
  return 1;
}

let b = a;

function a() {
  return 2;
}

console.log(a(), b());
```

---

# 🔥 **PROBLEM 18 — **

```js
const obj = {
  a: { x: 10 },
  b: { x: 20 }
};

let { a, b } = obj;

a.x = 99;
b = { x: 40 };

console.log(obj.a.x, obj.b.x, a.x, b.x);
```

---

# 🔥 **PROBLEM 19 — **

```js
function sum(arr) {
  if (arr.length === 0) return 0;
  return arr.pop() + sum(arr);
}

const nums = [1, 2, 3, 4];
console.log(sum(nums), nums.length);
```

---

# 🔥 **PROBLEM 20 — **

```js
let funcs = [];

for (var i = 0; i < 3; i++) {
  funcs.push(() => console.log(i));
}

funcs[0]();
funcs[1]();
funcs[2]();
```

---


