
# 🧠 **DAY 5 — PART 5.2 — ES5+ & ES6+ FEATURES (Deep + Simple)**

# 🟦 **5.2.1 — let vs const vs var**

---

### ✔ **var** (old way)

* Function-scoped
* Can redeclare
* Can reassign
* Causes bugs

```js
var a = 10;
var a = 20; // allowed
```

---

### ✔ **let** (modern)

* Block scoped
* Cannot redeclare
* Can update

```js
let x = 10;
x = 20; // allowed
```

---

### ✔ **const** (modern)

* Block scoped
* Cannot redeclare
* Cannot reassign
* Best for values that never change

```js
const PI = 3.14;
```

---

# 🟩 **5.2.2 — Template Literals (Backticks) ``**

Replaces ugly string concatenation.

Old way:

```js
let name = "Rahil";
console.log("Hello " + name + ", welcome!");
```

Modern way:

```js
console.log(`Hello ${name}, welcome!`);
```

Also supports multi-line strings:

```js
let msg = `
This is
a multi-line
string
`;
```

---

# 🟧 **5.2.3 — Arrow Functions (=>)**

Simple function:

```js
let sum = (a, b) => {
  return a + b;
};
```

Short version:

```js
let sum = (a, b) => a + b;
```

One parameter:

```js
let square = x => x * x;
```

No parameter:

```js
let greet = () => console.log("Hello!");
```

---

# 🟨 **5.2.4 — Default Parameters**

```js
function greet(name = "Guest") {
  console.log("Hello", name);
}

greet(); // Hello Guest
greet("Rahil"); // Hello Rahil
```

---

# 🟥 **5.2.5 — Spread Operator `...`**

Expands (spreads) array or object.

### Array example:

```js
let arr1 = [1, 2];
let arr2 = [...arr1, 3, 4];

console.log(arr2); // [1,2,3,4]
```

### Object example:

```js
let obj1 = { name: "Rahil" };
let obj2 = { ...obj1, age: 21 };

console.log(obj2);
```

---

# 🟪 **5.2.6 — Rest Operator `...`**

Collects remaining values.

```js
function sum(...numbers) {
  console.log(numbers);
}

sum(1, 2, 3, 4); // [1,2,3,4]
```

---

# 🟫 **5.2.7 — Destructuring (VERY IMPORTANT)**

### Array destructuring:

```js
let nums = [10, 20, 30];

let [a, b, c] = nums;

console.log(a, b, c);
```

Skip values:

```js
let [x, , z] = nums;
```

### Object destructuring:

```js
let user = {
  name: "Rahil",
  age: 21
};

let { name, age } = user;
```

Rename:

```js
let { name: username } = user;
```

---

# 🟦 **5.2.8 — Object Property Shorthand**

Old way:

```js
let name = "Rahil";
let age = 21;

let user = { name: name, age: age };
```

ES6 way:

```js
let user = { name, age };
```

---

# 🟩 **5.2.9 — for…of Loop (Modern Loop)**

Works on arrays, strings, NodeLists.

```js
let colors = ["red", "blue", "green"];

for (let c of colors) {
  console.log(c);
}
```

---

# 🟧 **5.2.10 — New String Methods**

```js
"hello".includes("he");  // true
"hey".startsWith("h");   // true
"bye".endsWith("e");     // false
```

---

# 🟨 **5.2.11 — New Array Methods**

We’ll go deep in 5.3, but here is quick intro:

```js
arr.includes(5)
arr.find()
arr.findIndex()
arr.at(-1)
```

---
