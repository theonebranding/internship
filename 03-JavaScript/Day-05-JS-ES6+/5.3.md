
# 🧠 **DAY 5 — PART 5.3 — MODERN ARRAY METHODS (Deep Dive)**

Modern array methods help you:

* Transform data
* Filter data
* Search lists
* Create new arrays
* Count totals
* Work with array of objects
* Handle API results

This is how 90% of real frontend apps work.

Let’s begin slowly and build up.

---

# 🎯 **5.3.1 — forEach() (Loop through Array)**

*(Does NOT return new array, just loops)*

### Syntax:

```js
array.forEach(function(item) {
  console.log(item);
});
```

### Example:

```js
let fruits = ["apple", "banana", "kiwi"];

fruits.forEach(f => console.log(f));
```

Used to **display items**, **render list**, etc.

---

# 🟦 **5.3.2 — map() (Transform Values)**

map = **create a new array** by transforming each item.

### Example 1 — Double numbers

```js
let nums = [1, 2, 3];

let doubled = nums.map(n => n * 2);

console.log(doubled); // [2,4,6]
```

### Example 2 — Extract property from array of objects

```js
let products = [
  { name: "Laptop", price: 50000 },
  { name: "Phone", price: 20000 }
];

let names = products.map(p => p.name);

console.log(names); // ["Laptop","Phone"]
```

Real-life use:

* Show product names
* Format data for UI
* Transform API data

---

# 🟩 **5.3.3 — filter() (Return items that pass a condition)**

### Example 1 — Numbers > 10

```js
let arr = [5, 10, 20, 30];

let big = arr.filter(n => n > 10);
console.log(big); // [20,30]
```

### Example 2 — Filter products < ₹30000

```js
let cheap = products.filter(p => p.price < 30000);
```

Real-life use:

* Search results
* Filter list by price/category
* Filter tasks by completed/incomplete

---

# 🟧 **5.3.4 — reduce() (Return a single value)**

reduce = **total / count / sum / combine**.

### Example 1 — Sum of array

```js
let nums = [10, 20, 30];

let total = nums.reduce((sum, n) => sum + n, 0);

console.log(total); // 60
```

### Example 2 — Total cart amount (real-life)

```js
let cart = [
  { product: "Shoes", price: 2000, qty: 2 },
  { product: "Watch", price: 1500, qty: 1 }
];

let totalBill = cart.reduce((sum, item) => {
  return sum + item.price * item.qty;
}, 0);

console.log(totalBill); // 5500
```

Real-life use:

* Billing
* Summaries
* Analytics
* Statistics

---

# 🟨 **5.3.5 — find() (Find first match)**

### Example:

```js
let nums = [5, 10, 20, 30];

let found = nums.find(n => n > 10);

console.log(found); // 20
```

Real-life:

* Find user by ID
* Find product by name
* Find order by orderId

---

# 🟪 **5.3.6 — findIndex()**

```js
let index = nums.findIndex(n => n === 20);
console.log(index); // 2
```

---

# 🟫 **5.3.7 — some() (At least one passes?)**

```js
let arr = [5, 10, 20];

console.log(arr.some(n => n > 15)); // true
```

Real-life:

* Check if any product is out of stock
* Check if any user is inactive

---

# 🟫 **5.3.8 — every() (All pass?)**

```js
let nums = [10, 20, 30];

console.log(nums.every(n => n > 5)); // true
```

Real-life:

* Check if all students passed
* Check if all products are in stock

---

# 🟦 **5.3.9 — at() (New feature)**

Access from end cleanly.

```js
let arr = [10, 20, 30, 40];

console.log(arr.at(-1)); // 40 (last item)
console.log(arr.at(-2)); // 30
```

---

# 🟩 **5.3.10 — sort() (Be careful)**

Sort numbers:

```js
let nums = [20, 3, 100];

nums.sort((a, b) => a - b); // proper numeric sorting

console.log(nums); // [3,20,100]
```

Sort objects by price:

```js
products.sort((a, b) => a.price - b.price);
```

---

# 🌍 **5.3.11 — Real-Life Examples (Highly Practical)**

---

### ✔ Example A — Search Users by Name

```js
let users = [
  {name:"Rahil", age:21},
  {name:"Sana", age:22},
  {name:"Ayan", age:20}
];

let result = users.filter(u => u.name.includes("a"));
```

---

### ✔ Example B — Get Emails from User List

```js
let emails = users.map(u => u.email);
```

---

### ✔ Example C — Calculate Average Marks

```js
let marks = [80, 90, 70, 60];

let avg = marks.reduce((s, m) => s + m, 0) / marks.length;
```

---

### ✔ Example D — Remove Out-of-Stock Products

```js
products = products.filter(p => p.stock > 0);
```

---

### ✔ Example E — Convert All Product Names to Uppercase

```js
let names = products.map(p => p.name.toUpperCase());
```

---

# 🎉 **5.3 Complete — You Now Know Professional Array Methods**

What you learned today is used in:

✔ React (map to display components)
✔ Node.js APIs
✔ Filtering tables and data
✔ Billing and dashboards
✔ Searching, sorting, transforming data

This is REAL JavaScript.
