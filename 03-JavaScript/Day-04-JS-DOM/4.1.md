
# 🧠 **DAY 4 — PART 4.1 — DOM BASICS**

# **Understanding the DOM (Deep but Simple)**

---

# 🎯 **4.1.1 — What is the DOM? (Plain English)**

The **DOM (Document Object Model)** is a *bridge* between:

✔ your HTML page
✔ your JavaScript code

When the browser loads a webpage:

➡ It reads your HTML
➡ Builds a **DOM tree**
➡ JavaScript uses this tree to **add, remove, update** anything on the screen.

Think of DOM as a digital representation of your webpage.

---

# 🌳 **4.1.2 — DOM as a Tree (Very Important)**

HTML:

```html
<body>
  <h1>Hello</h1>
  <p>Welcome</p>
</body>
```

Browser creates a **DOM tree**:

```
Document
 └── html
      └── body
           ├── h1
           └── p
```

JavaScript interacts with nodes of this tree.

---

# 🟦 **4.1.3 — Why We Need the DOM?**

To make web pages **dynamic**.

With DOM + JS you can:

✔ change text
✔ change colors
✔ show/hide elements
✔ move elements
✔ add new elements
✔ delete elements
✔ add animations
✔ react to user input
✔ build apps (todo, ecommerce, chat, etc.)

Without DOM, JS cannot touch HTML.

---

# 🧩 **4.1.4 — Where to Write JavaScript in HTML?**

Inside `<script>` tag:

### Option 1: Inside HTML file

```html
<script>
  console.log("Hello DOM");
</script>
```

### Option 2: In external JS file (recommended)

```html
<script src="app.js"></script>
```

---

# 🟩 **4.1.5 — DOM Methods to Select Elements (VERY IMPORTANT)**

To change something, you must **select** it first.

### ✔ `getElementById()`

```js
document.getElementById("title");
```

### ✔ `getElementsByClassName()` (returns list)

```js
document.getElementsByClassName("item");
```

### ✔ `getElementsByTagName()`

```js
document.getElementsByTagName("p");
```

### ✔ `querySelector()` (BEST — selects first match)

```js
document.querySelector(".box");
document.querySelector("#title");
```

### ✔ `querySelectorAll()` (selects all)

```js
document.querySelectorAll(".item");
```

---

# 🟥 **4.1.6 — Modifying Content**

### ✔ Change text

```js
document.getElementById("title").textContent = "New Title";
```

### ✔ Change HTML inside element

```js
document.getElementById("box").innerHTML = "<b>Bold Text</b>";
```

---

# 🟧 **4.1.7 — Modifying Style**

```js
let box = document.querySelector(".box");
box.style.backgroundColor = "blue";
box.style.color = "white";
box.style.padding = "20px";
```

Note: Style properties use **camelCase** in JS.
(E.g. background-color → backgroundColor)

---

# 🟨 **4.1.8 — Creating Elements using JS**

```js
let newDiv = document.createElement("div");
newDiv.textContent = "I was created using JS!";
document.body.appendChild(newDiv);
```

---

# 🟪 **4.1.9 — Removing Elements**

```js
let item = document.getElementById("remove-me");
item.remove();
```

---

# 🟫 **4.1.10 — Real-Life DOM Examples**

### 🛒 Change product price

```js
document.querySelector(".price").textContent = "$999";
```

### 🔔 Show notification

```js
document.querySelector(".notification").style.display = "block";
```

### 📝 Add a new todo item

```js
let li = document.createElement("li");
li.textContent = "New Task";
document.querySelector("ul").appendChild(li);
```

### 📦 Remove an item from cart

```js
document.querySelector(".cart-item").remove();
```
