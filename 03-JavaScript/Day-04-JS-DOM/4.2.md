
# 🧠 **DAY 4 — PART 4.2 — SELECTING & MODIFYING HTML (Deep & Practical)**

We will cover:

✔ Selecting elements (all 6 methods)
✔ Changing text & HTML
✔ Changing style
✔ Changing attributes
✔ Adding/Removing classes
✔ Creating elements
✔ Appending & prepending
✔ Removing elements
✔ Real-life examples

Lots of examples so it stays beginner-friendly.

---

# 🔍 **4.2.1 — Selecting Elements (VERY IMPORTANT)**

You first SELECT → then MODIFY.

---

## ✔ `getElementById()`

Select by ID
(IDs are unique)

```html
<h1 id="title">Hello</h1>
```

```js
let heading = document.getElementById("title");
```

---

## ✔ `getElementsByClassName()`

Returns **HTMLCollection** (like an array but not exactly)

```html
<p class="info">One</p>
<p class="info">Two</p>
```

```js
let items = document.getElementsByClassName("info");
console.log(items[0]);
```

---

## ✔ `getElementsByTagName()`

```js
let buttons = document.getElementsByTagName("button");
```

---

## ✔ `querySelector()` (BEST — most used)

Selects the **first matching** element.

```js
document.querySelector(".box");  
document.querySelector("#title");  
document.querySelector("p");  
```

---

## ✔ `querySelectorAll()`

Selects **all matching elements** (NodeList)

```js
let allItems = document.querySelectorAll(".item");
```

---

# 🟦 **4.2.2 — Changing Text**

## ✔ textContent (changes plain text)

```js
document.querySelector("#title").textContent = "New Heading";
```

## ✔ innerText (similar but respects CSS)

```js
title.innerText = "Hidden items ignored";
```

---

# 🟩 **4.2.3 — Changing HTML inside element**

## ✔ innerHTML (injects HTML)

```js
document.querySelector("#box").innerHTML = "<b>Bold Text</b>";
```

Real-life use → loading dynamic cards from backend.

---

# 🟧 **4.2.4 — Changing Styles with JS**

```js
let box = document.querySelector(".box");

box.style.backgroundColor = "red";
box.style.color = "white";
box.style.padding = "20px";
box.style.borderRadius = "8px";
```

Note: In JS, CSS uses camelCase.

---

# 🟨 **4.2.5 — Adding & Removing Classes**

Works well with CSS.

```js
let card = document.querySelector(".card");

card.classList.add("active");
card.classList.remove("hidden");
card.classList.toggle("dark-mode");
```

Real-life:

* Night mode
* Open/close menu
* Validation states

---

# 🟥 **4.2.6 — Get & Set HTML Attributes**

### Get attribute

```js
let link = document.querySelector("a");
console.log(link.getAttribute("href"));
```

### Set attribute

```js
link.setAttribute("href", "https://google.com");
```

### Remove attribute

```js
link.removeAttribute("target");
```

Real-life:

* Update image source
* Update link
* Add alt text

---

# 🟪 **4.2.7 — Creating Elements with JS**

```js
let newDiv = document.createElement("div");
newDiv.textContent = "Hello!";
```

### Add to page

```js
document.body.appendChild(newDiv);
```

---

# 🟫 **4.2.8 — Append vs Prepend**

## appendChild() → adds at end

```js
list.appendChild(newItem);
```

## prepend() → adds at start

```js
list.prepend(newItem);
```

---

# 🟩 **4.2.9 — Remove Elements**

```js
document.querySelector(".item").remove();
```

---

# 🌍 **4.2.10 — Real-Life DOM Examples**

---

## ✔ Example 1 — Update a counter on website

```html
<p id="count">0</p>
```

```js
let count = document.getElementById("count");
count.textContent = 5;
```

---

## ✔ Example 2 — Change theme (light → dark)

```js
document.body.classList.toggle("dark");
```

---

## ✔ Example 3 — Show/hide a section

```js
document.querySelector(".modal").style.display = "block";
```

---

## ✔ Example 4 — Add a new todo item

```js
let li = document.createElement("li");
li.textContent = "Buy Milk";

document.querySelector("ul").appendChild(li);
```

---

## ✔ Example 5 — Update user profile picture

```js
document.querySelector("img").setAttribute("src", "new-photo.jpg");
```

---

## ✔ Example 6 — Change button text on click

```js
document.querySelector("button").textContent = "Loading...";
```

---

## ✔ Example 7 — Delete all items from list

```js
document.querySelector("ul").innerHTML = "";
```