# 🧠 **DAY 1 — PART 1.2**

# **JavaScript Basics: Variables, Data Types, Output, Operators (Deep & Practical)**

This section builds the *foundation* for everything you’ll do in JS — React, Node, APIs, DOM, backend — all rely on these fundamentals.

Let’s go step by step.

---

# 🌱 **1.2.1 — What is a Variable?**

A **variable** is a box where you store information.
You give the box a name → put a value inside it → use it later.

Think of variables like:

* storing a user’s name
* keeping score in a game
* saving a product price
* tracking login status

### There are 3 ways to declare variables:

```js
var
let
const
```

---

## ✔ **let** (most common)

* Can change value
* Block scoped
* Safe

```js
let age = 20;
age = 21; // allowed
```

Real life use:

```js
let itemsInCart = 3;
itemsInCart = itemsInCart + 1; // user adds one item
```

---

## ✔ **const** (for fixed values)

* Cannot be reassigned
* Use for values that never change

```js
const pi = 3.14159;
```

Real life use:

```js
const apiBaseUrl = "https://api.example.com/";
```

---

## ✔ **var** (older, avoid using)

* Function scoped
* Causes bugs, avoid in modern JS

```js
var name = "Rahil";
```

---

### 🔥 Best practice:

**Use `let` for normal variables and `const` for constants.
Avoid `var`.**

---

# 🎭 **1.2.2 — JavaScript Data Types (Deep but Simple)**

JavaScript has two major categories:

## 1️⃣ **Primitive types** (simple, single values)

* `string`
* `number`
* `boolean`
* `undefined`
* `null`
* `bigint`
* `symbol`

## 2️⃣ **Non-primitive types**

* `object`

  * arrays
  * functions
  * objects

Let’s explore with examples.

---

# 🔤 **Strings** (text)

```js
let username = "Rahil";
let msg = 'Hello World';
```

Real life:

```js
let welcomeText = "Welcome to our website!";
```

---

# 🔢 **Numbers**

JS uses one number type (no int/float difference).

```js
let price = 499.99;
let age = 21;
```

Real life:

```js
let totalAmount = price * 2;
```

---

# ✔ **Boolean**

True or false.

```js
let isLoggedIn = true;
let hasPremium = false;
```

Real life:

```js
if (isLoggedIn) {
  console.log("Welcome!");
}
```

---

# ❓ **undefined**

Variable declared but no value assigned.

```js
let x;
console.log(x); // undefined
```

---

# ⛔ **null**

Intentional “empty” value.

```js
let selectedProduct = null;
```

Real life:

```js
let activeChat = null; // no chat selected
```

---

# 🔢 **bigint**

For very big numbers (rare for interns)

```js
let big = 1234567890123456789n;
```

---

# 🪪 **symbol**

Unique identifiers (used in advanced JS)

```js
let id = Symbol("id");
```

---

# 🧱 **Objects** (key-value data)

```js
let user = {
  name: "Rahil",
  age: 21,
  job: "Developer"
};
```

Real life:

```js
let product = {
  name: "iPhone 14",
  price: 70000,
  inStock: true
};
```

---

# 📦 **Arrays**

Ordered lists.

```js
let fruits = ["apple", "banana", "orange"];
```

Real life:

```js
let cart = ["shoes", "shirt", "camera"];
```

---

# 🧰 **Functions** (we’ll learn tomorrow but just know they are data type too)

```js
function greet() {
  console.log("Hello");
}
```

---

# 🖨️ **1.2.3 — Input & Output in JS**

## 🖨️ **Console Output**

Used for debugging.

```js
console.log("JS is working");
```

---

## 🖥️ **Browser Popups (not recommended for production)**

### `alert()`

```js
alert("Welcome!");
```

### `prompt()`

Get user input:

```js
let name = prompt("Enter name:");
console.log(name);
```

### `confirm()`

Yes/No dialog:

```js
let response = confirm("Are you sure?");
```

Real life:

```js
if (confirm("Delete item?")) {
  console.log("Item deleted");
}
```

---

# ➕ **1.2.4 — Operators (Deep but Simple)**

## 🔹 Arithmetic Operators

```js
+  -  *  /  %  **
```

Examples:

```js
let total = 10 + 20;
let area = 5 * 4;
let remainder = 10 % 3;
let power = 2 ** 3;
```

Real life:

```js
let discountPrice = price - (price * 0.10);
```

---

## 🔁 Assignment Operators

```js
=
+=
-=
*=
/=
```

Example:

```js
let x = 10;
x += 5;  // x = x + 5
```

---

## 🔍 Comparison Operators

### Loose equality (checks value)

```js
5 == "5" // true
```

### Strict equality (checks type + value)

```js
5 === "5" // false
```

### Inequality

```js
!=
!== 
```

### Greater/less

```js
>
<
>=
<=
```

Real life:

```js
if (userAge >= 18) {
  console.log("Allowed");
}
```

---

## 🤖 Logical Operators

```js
&&  // AND
||  // OR
!   // NOT
```

Real life:

```js
if (isLoggedIn && hasPremium) {
  console.log("Show premium content");
}
```

---

# 🧩 **1.2.5 — Real-Life Code Examples**

## 🛒 Example: Product Price Calculation

```js
let price = 499;
let qty = 3;

let total = price * qty;
console.log("Total Bill:", total);
```

---

## 🧍 User Greeting Example

```js
let name = prompt("Enter your name:");
console.log("Welcome " + name + "!");
```

---

## 🚦 Check Login Status

```js
let isLoggedIn = true;

if (isLoggedIn) {
  console.log("Dashboard opened");
} else {
  console.log("Please login");
}
```

---

## 🥤 Example: Discount System

```js
let amount = 1200;

if (amount > 1000) {
  console.log("You get 10% off");
} else {
  console.log("No discount");
}
```

---
