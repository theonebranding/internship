

## 🌀 What is a Conic Gradient?

A **conic gradient** smoothly transitions colors **around a circle**, rotating like a clock face.

Basic syntax:

```css
background: conic-gradient(from angle at position, color1, color2, ...);
```

**Parameters:**

* `from angle` — optional starting angle (default: `0deg`, or the top)
* `at position` — optional origin point (default: `center`)
* `color1, color2...` — the colors that will rotate around the center

---

## 🎨 Example 1: Basic Conic Gradient

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Conic Gradient Example</title>
  <style>
    .box {
      width: 250px;
      height: 250px;
      border-radius: 50%;
      background: conic-gradient(red, yellow, green, blue, purple, red);
      margin: 40px auto;
    }
  </style>
</head>
<body>
  <div class="box"></div>
</body>
</html>
```

🧩 Here the colors spin in a circle, creating a **color wheel effect** — notice the colors blend as they go around, not across or outward.

---

## 🔁 Example 2: Controlling the Start Angle

You can rotate where the gradient begins using the `from` keyword:

```css
background: conic-gradient(from 90deg, red, yellow, green, blue);
```

This makes the first color (`red`) start **at the right side** (90 degrees), rather than the top (0 degrees).

---

## 🎯 Example 3: Changing the Center Point

You can shift the center of rotation using `at`:

```css
background: conic-gradient(at top left, red, yellow, green, blue);
```

Now the gradient rotates around the **top-left corner** of the box — this creates a cool asymmetrical look.

---

## 🍕 Example 4: Creating Pie Chart Segments

Conic gradients are perfect for creating **charts and circular indicators** — no JavaScript required.

```html
<style>
  .pie {
    width: 250px;
    height: 250px;
    border-radius: 50%;
    background: conic-gradient(
      #ff6384 0% 25%,   /* red slice */
      #36a2eb 25% 50%,  /* blue slice */
      #ffce56 50% 75%,  /* yellow slice */
      #4bc0c0 75% 100%  /* teal slice */
    );
    margin: 40px auto;
  }
</style>

<div class="pie"></div>
```

🎯 **Use Case:** You can visualize data like “tasks completed”, “budget distribution”, or “survey results” — all with pure CSS!

---

## ✨ Example 5: Repeating Conic Gradient

`repeating-conic-gradient()` lets you create **patterns**, like spinning stripes or backgrounds.

```css
background: repeating-conic-gradient(
  black 0deg 10deg,
  white 10deg 20deg
);
```

That creates a **black and white pinwheel** effect — useful for retro spinners, dials, or hypnotic effects.

---

## 🌈 Real-Life Example: Loading Spinner

```html
<style>
  .spinner {
    width: 150px;
    height: 150px;
    border-radius: 50%;
    background: conic-gradient(from 0deg, #4facfe, #00f2fe, #4facfe);
    animation: spin 2s linear infinite;
    margin: 40px auto;
  }

  @keyframes spin {
    to { transform: rotate(360deg); }
  }
</style>

<div class="spinner"></div>
```

Here, the **conic gradient** creates a nice gradient ring that spins infinitely — like a loading icon you’d see on real websites.

---

## 📘 Summary Table

| Property / Function          | Description                                           | Example                                                  |
| ---------------------------- | ----------------------------------------------------- | -------------------------------------------------------- |
| `conic-gradient()`           | Creates a gradient that rotates around a center point | `conic-gradient(red, yellow, blue)`                      |
| `from angle`                 | Sets starting point (rotation)                        | `from 90deg`                                             |
| `at position`                | Sets gradient origin                                  | `at top left`                                            |
| `repeating-conic-gradient()` | Repeats colors for striped effects                    | `repeating-conic-gradient(red 0 10deg, yellow 10 20deg)` |

---

## 🧠 Where It’s Used in Real Life

* **Pie charts** or **donut charts** (data visualization)
* **Progress rings** or **loaders**
* **Abstract or dynamic backgrounds**
* **Circular lighting or spotlight effects**

---
