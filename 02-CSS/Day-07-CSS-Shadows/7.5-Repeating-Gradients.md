
# 🎨 **CSS Gradients — The Complete Guide**

Gradients let you blend two or more colors **smoothly** without using any images.
They’re dynamic, lightweight, and fully customizable — perfect for backgrounds, buttons, banners, or entire page designs.

There are **three main types of gradients** in CSS:

1. **Linear Gradient** — colors move in a straight line
2. **Radial Gradient** — colors radiate outward in circles or ellipses
3. **Conic Gradient** — colors spin around a center point

And each type can have a **repeating** version that creates patterns.

---

## 🌈 1. **Linear Gradients**

A **linear gradient** moves colors along a straight line — horizontally, vertically, or diagonally.

### Syntax:

```css
background: linear-gradient(direction, color1, color2, ...);
```

### Example:

```html
<div class="linear"></div>

<style>
.linear {
  height: 200px;
  background: linear-gradient(to right, red, yellow);
}
</style>
```

Here, the color blends **from red (left)** to **yellow (right)**.

You can also use **angles** instead of directions:

```css
background: linear-gradient(45deg, red, yellow);
```

`45deg` means the blend moves diagonally from top-left to bottom-right.

---

## 🧭 2. **Radial Gradients**

A **radial gradient** radiates from a **center point**, like a light or ripple.

### Syntax:

```css
background: radial-gradient(shape size at position, color1, color2, ...);
```

### Example:

```html
<div class="radial"></div>

<style>
.radial {
  height: 200px;
  background: radial-gradient(circle at center, red, yellow, green);
}
</style>
```

You can adjust:

* Shape → `circle` or `ellipse`
* Size → `closest-side`, `farthest-corner`, etc.
* Position → `at top left`, `at 50% 30%`

Real-life uses: **glows**, **spotlights**, **soft backgrounds**, **light sources**.

---

## 🌀 3. **Conic Gradients**

A **conic gradient** rotates colors around a **center point**, like slices of a pie chart.

### Syntax:

```css
background: conic-gradient(from angle at position, color1, color2, ...);
```

### Example:

```html
<div class="conic"></div>

<style>
.conic {
  height: 200px;
  width: 200px;
  border-radius: 50%;
  background: conic-gradient(red, yellow, green, blue, red);
  margin: 20px;
}
</style>
```

Use cases:

* **Pie charts**
* **Progress rings**
* **Circular color transitions**
* **Spinners**

You can also set where the rotation begins:

```css
background: conic-gradient(from 90deg, red, yellow, blue);
```

---

## 🔁 4. **Repeating Gradients**

All gradient types can repeat to form **patterns or textures**.

---

### (a) Repeating Linear Gradient

```css
background: repeating-linear-gradient(
  45deg,
  red 0px,
  red 10px,
  yellow 10px,
  yellow 20px
);
```

This creates diagonal **striped lines** repeating every 20 pixels.

---

### (b) Repeating Radial Gradient

```css
background: repeating-radial-gradient(
  circle,
  red 0px,
  yellow 10px,
  red 20px
);
```

This produces concentric circles, perfect for **ripple or bullseye effects**.

---

### (c) Repeating Conic Gradient

```css
background: repeating-conic-gradient(
  black 0deg 10deg,
  white 10deg 20deg
);
```

This creates **spinning pinwheel** or **clock-like patterns** — often used for spinners or funky backgrounds.

---

## 🌟 5. **Combined Gradient Example**

Let’s mix them all in one page.

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Gradients Together</title>
  <style>
    body {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      justify-content: center;
      background-color: #f0f0f0;
      padding: 20px;
      font-family: Arial, sans-serif;
    }

    .box {
      width: 250px;
      height: 200px;
      border-radius: 12px;
      border: 2px solid #ccc;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
    }

    .linear {
      background: linear-gradient(to right, #ff9a9e, #fad0c4);
    }

    .radial {
      background: radial-gradient(circle at center, #a1c4fd, #c2e9fb);
    }

    .conic {
      background: conic-gradient(from 90deg, #ff6a00, #ee0979, #ff6a00);
    }

    .repeating {
      background: repeating-linear-gradient(
        45deg,
        #84fab0 0px,
        #8fd3f4 10px,
        #84fab0 20px
      );
    }
  </style>
</head>
<body>
  <div class="box linear"></div>
  <div class="box radial"></div>
  <div class="box conic"></div>
  <div class="box repeating"></div>
</body>
</html>
```

Each box shows a different type of gradient — linear, radial, conic, and repeating.

---

## 🧠 6. Summary Table

| Type          | Function                | Direction                    | Common Use           |
| ------------- | ----------------------- | ---------------------------- | -------------------- |
| **Linear**    | `linear-gradient()`     | Along a line (0°, 90°, etc.) | Backgrounds, banners |
| **Radial**    | `radial-gradient()`     | Outward from a center        | Glows, spotlights    |
| **Conic**     | `conic-gradient()`      | Around a circle              | Charts, spinners     |
| **Repeating** | `repeating-*gradient()` | Repeats patterns             | Textures, stripes    |

---

## 💡 Real-World Uses of Gradients

* **Backgrounds** for websites, cards, and buttons
* **Overlay effects** on images
* **Charts** and **progress circles**
* **Hover transitions** for buttons
* **Modern UI design** (soft color blends, glowing borders)

---
