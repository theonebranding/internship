

## 🌤️ **Task 1: Create a “Shadow Button Set”**

### 🎯 Goal:

Design three buttons — **default**, **hover**, and **active (pressed)** — each using different shadow styles to represent depth changes.

### 💡 Requirements:

* Use `box-shadow` to make the button look raised in the default state.
* Make it look like it “lifts up” slightly when hovered (lighter, wider shadow).
* Make it look “pressed in” when active (use `inset`).

### 🧩 Example Starter Code:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Shadow Buttons</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f5f5f5;
      text-align: center;
      padding: 50px;
    }

    .btn {
      background: #3f51b5;
      color: white;
      border: none;
      padding: 15px 30px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      font-size: 16px;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .btn:hover {
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.25);
      transform: translateY(-2px);
    }

    .btn:active {
      box-shadow: inset 0 4px 6px rgba(0, 0, 0, 0.3);
      transform: translateY(2px);
    }
  </style>
</head>
<body>
  <button class="btn">Shadow Button</button>
</body>
</html>
```

## Output
<img width="867" height="761" alt="image" src="https://github.com/user-attachments/assets/6cf2b738-54f1-4ebe-89fa-2350b8d177dc" />


### 🧠 Tasks Challenge:

Make **three versions**:

1. A **primary button** with a blue theme (like above).
2. A **danger button** in red.
3. A **success button** in green.
   Each should have its own shadow color and behavior.

---

## 🌌 **Task 2: Build a “Neon Text Banner”**

### 🎯 Goal:

Use multiple `text-shadow` layers to create a glowing neon sign effect.

### 💡 Requirements:

* Text color should be white or light.
* Background should be dark.
* Use **at least two text-shadow layers** in different colors.
* Animate the glow slightly using CSS keyframes for bonus points.

### 🧩 Example Starter Code:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Neon Text</title>
  <style>
    body {
      background-color: #000;
      color: white;
      text-align: center;
      padding-top: 100px;
      font-family: 'Poppins', sans-serif;
    }

    .neon {
      font-size: 60px;
      color: #fff;
      text-shadow:
        0 0 5px #00e6e6,
        0 0 10px #00e6e6,
        0 0 20px #00cccc,
        0 0 40px #00cccc;
      animation: flicker 1.5s infinite alternate;
    }

    @keyframes flicker {
      from {
        text-shadow:
          0 0 5px #00e6e6,
          0 0 10px #00e6e6,
          0 0 20px #00cccc,
          0 0 40px #00cccc;
      }
      to {
        text-shadow:
          0 0 2px #00e6e6,
          0 0 5px #00e6e6,
          0 0 10px #00cccc,
          0 0 20px #00cccc;
      }
    }
  </style>
</head>
<body>
  <h1 class="neon">Neon Nights</h1>
</body>
</html>
```
## Output
<img width="821" height="513" alt="image" src="https://github.com/user-attachments/assets/3664806a-7fca-4706-89da-113f49efb414" />

