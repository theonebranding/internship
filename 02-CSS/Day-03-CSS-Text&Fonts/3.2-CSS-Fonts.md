## 🌿 What Are Fonts in CSS?

**Fonts** control how the text looks — not just what it says.
CSS lets you set:

* **Font family** (which font to use)
* **Font size**
* **Font weight** (boldness)
* **Font style** (normal or italic)
* **Font variant** (small-caps)
* **Line height**
* **Font shorthand** (to combine all above)

---

## 🧩 Font Family

This defines which typeface to use. You can list multiple fonts — if the browser can’t load the first one, it tries the next.

```html
<h1 class="heading">Welcome to My Website</h1>
<p class="text">Fonts make your page come alive!</p>
```

```css
.heading {
  font-family: "Poppins", "Verdana", sans-serif;
}

.text {
  font-family: "Times New Roman", serif;
}
```

### Common font families

* **Serif**: Has small decorative lines — good for print (e.g. *Times New Roman*).
* **Sans-serif**: Clean and modern — common on screens (e.g. *Arial, Poppins*).
* **Monospace**: Equal width characters — used for code (e.g. *Courier New*).
* **Cursive**: Handwriting-like (e.g. *Brush Script MT*).
* **Fantasy**: Decorative and creative (e.g. *Papyrus*).

---

## 🔠 Font Size

Controls how big or small your text appears.

```css
p {
  font-size: 18px;   /* fixed size */
}

h1 {
  font-size: 2em;    /* relative to parent font size */
}

span {
  font-size: 150%;   /* percentage of parent size */
}
```

**Common units:**

* `px` → fixed pixel value
* `em` → relative to the parent’s font size
* `rem` → relative to root (html) font size
* `%` → relative to parent element

---

## 💪 Font Weight

Controls the **boldness** of text.

```css
h2 {
  font-weight: bold;     /* same as 700 */
}

p {
  font-weight: 400;      /* normal */
}

span {
  font-weight: 900;      /* very bold */
}
```

You can use **keywords** (`normal`, `bold`, `lighter`) or **numbers** (100–900).

---

## ✨ Font Style

Used for italics or oblique text.

```css
em {
  font-style: italic;
}

.note {
  font-style: oblique;
}
```

---

## 🧬 Font Variant

Used for small-caps styling (uppercase letters but smaller for lowercase text).

```css
h3 {
  font-variant: small-caps;
}
```

---

## 🪜 Line Height

Controls the **space between lines of text**.

```css
p {
  line-height: 1.6;   /* 1.6 times the font size */
}
```

A good line height improves readability.

---

## 🧾 Font Shorthand Property

You can combine multiple font properties in one line:

```css
p {
  font: italic small-caps bold 18px/1.5 "Poppins", sans-serif;
}
```

Order:
`font: font-style font-variant font-weight font-size/line-height font-family;`

---

## 🌈 Example — Font Styling Playground

```html
<!DOCTYPE html>
<html>
<head>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f9f9f9;
      padding: 20px;
    }

    h1 {
      font-family: "Georgia", serif;
      font-size: 36px;
      font-weight: bold;
      color: #333;
    }

    p {
      font-size: 18px;
      line-height: 1.6;
      font-style: italic;
      font-variant: small-caps;
      color: #444;
    }

    .note {
      font: normal small-caps 600 20px/1.8 "Courier New", monospace;
      color: #0066cc;
    }
  </style>
</head>
<body>
  <h1>CSS Font Styling Example</h1>
  <p>Fonts can completely change how a webpage feels and reads.</p>
  <p class="note">Experiment with different font families, sizes, and weights!</p>
</body>
</html>
```

--- 


Perfect timing — fonts are where **design personality** really shines. Let’s go deeper into **font-family** and learn how to use **Google Fonts**, because using just “Arial” and “Times New Roman” forever is like living life with only two crayons.

---

## 🧩 Understanding Font Family More Deeply

When you write:

```css
font-family: "Poppins", "Verdana", sans-serif;
```

you’re actually giving the browser a **font fallback list**.

* `"Poppins"` → your *first choice font*.
* `"Verdana"` → used if `"Poppins"` isn’t available.
* `sans-serif` → the **generic family**, used if neither of the above are available.

### Why do we use multiple fonts?

Because users have different devices and systems — some may not have your first choice font installed. So you list fonts from most preferred to most generic.

---

### 🎨 The Five Generic Font Families

| Family         | Description                           | Example Fonts             |
| -------------- | ------------------------------------- | ------------------------- |
| **serif**      | Letters have small decorative strokes | Times New Roman, Georgia  |
| **sans-serif** | Clean, no decorative lines            | Arial, Helvetica, Poppins |
| **monospace**  | Each character takes equal space      | Courier New, Consolas     |
| **cursive**    | Script or handwriting-like fonts      | Brush Script MT           |
| **fantasy**    | Decorative or playful                 | Papyrus, Jokerman         |

---

## 💡 Example: Font Family Fallback

```css
body {
  font-family: "Times New Roman", Georgia, serif;
}

h1 {
  font-family: "Poppins", "Helvetica", sans-serif;
}
```

If your computer doesn’t have *Poppins*, it’ll show *Helvetica* instead, and if that’s missing too — it falls back to any available **sans-serif** font.

---

## 🌐 Using Google Fonts (Modern Way)

Google Fonts is a free library of hundreds of web fonts that you can use easily on any site.

### Step 1: Go to

[https://fonts.google.com](https://fonts.google.com)

### Step 2: Choose a font

For example, let’s pick **Poppins** and **Roboto**.

### Step 3: Copy the `<link>` tag they give

Add it inside your HTML `<head>`:

```html
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&family=Roboto:wght@300;400&display=swap" rel="stylesheet">
```

### Step 4: Use it in your CSS

```css
body {
  font-family: "Roboto", sans-serif;
}

h1, h2 {
  font-family: "Poppins", sans-serif;
  font-weight: 600;
}
```

That’s it! Google hosts the fonts on their servers — the browser automatically downloads them.

---

## ⚙️ Alternate Way — Using `@import`

Instead of a `<link>`, you can also import fonts directly into your CSS file:

```css
@import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&display=swap');

h1 {
  font-family: 'Playfair Display', serif;
}
```

### Difference:

* `<link>` → Better for performance (recommended)
* `@import` → Works inside CSS but loads slower

---

## 🧾 Example: Using Multiple Google Fonts

```html
<!DOCTYPE html>
<html>
<head>
  <title>Google Fonts Example</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&family=Roboto:wght@300;400&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: "Roboto", sans-serif;
      background-color: #f4f4f9;
      color: #333;
      padding: 20px;
    }

    h1 {
      font-family: "Poppins", sans-serif;
      font-weight: 600;
      color: #1e90ff;
    }

    p {
      font-size: 18px;
      line-height: 1.6;
    }

    .quote {
      font-family: "Poppins", serif;
      font-style: italic;
      color: #555;
      border-left: 4px solid #1e90ff;
      padding-left: 10px;
    }
  </style>
</head>
<body>
  <h1>Using Google Fonts</h1>
  <p>Google Fonts make it easy to use stylish typefaces without installing anything.</p>
  <p class="quote">"Good typography is invisible, bad typography is everywhere."</p>
</body>
</html>
```

---

## 🧠 Pro Tips

* Combine **two fonts** at most — one for headings (like *Poppins*) and one for text (like *Roboto*). Too many fonts look messy.
* Always include a **generic fallback** (`sans-serif`, `serif`), in case the web font fails to load.
* To make text smoother on screens, browsers automatically optimize how fonts render.

---



Fonts are where **art meets readability**.
Up next, we’ll explore **buttons and interactive states** — hover, active, and visited effects that make your pages come alive like a living interface.
